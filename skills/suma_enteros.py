# apa/skills/suma_enteros.py

SKILL = {'name': 'suma_enteros', 'keywords': ['suma', 'enteros', 'tipado', 'funcion', 'prueba', 'main', 'type hints'], 'prompt_fragment': "Implementar función suma(a, b) que retorna la suma de dos enteros usando type hints para asegurar tipos claros. Incluir bloque main con validación de criterio de aceptación (suma(2, 3) == 5) y manejo de excepciones. Usar if __name__ == '__main__' para ejecución segura. Priorizar claridad, test básico inline y robustez contra errores inesperados. Mantener código simple, legible y autoexplicativo.", 'example_code': 'from typing import Union\ndef suma(a: int, b: int) -> int:\n    # Retorna la suma de dos enteros\n    return a + b\ndef main() -> None:\n    try:\n        resultado = suma(2, 3)\n        if resultado == 5:\n            print("CRITERIO OK")\n        else:\n            print(f"CRITERIO FALLO: se esperaba 5, se obtuvo {resultado}")\n    except Exception as e:\n        print(f"CRITERIO FALLO: {e}")\nif __name__ == "__main__":\n    main()'}
